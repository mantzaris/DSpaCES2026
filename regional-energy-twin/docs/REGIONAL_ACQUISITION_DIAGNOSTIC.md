# Acquisition diagnosis protocol - 2026-09-23

Authorized continuation of 585bc42. Preserve all Stage 3 outcomes. No acquisition
variant has been selected at this protocol's creation. Reconstruct missing
intermediates with the unchanged GPU coarse model, not a full comparative replay.
Verify each step against saved M3 ordered meter identities, returned masks, active
group and maximum score. The reconstruction is new computation, not a recovered
original trace. It uses the actual physical meter ID and timestamp in hashes;
ordered and canonical hashes distinguish order from set equality.

Original path: acquire 41 fixed rotating probes; form absolute standardized
innovations against the previous aggregate-only one-step predictive distribution;
update q_g=.8 q_g + max(0,max acquired-probe z_i -2); if max q>13.4333766777,
activate argmax for four subsequent hold decisions. Acquire 42 exploratory and
126 selected-group readings (fill if needed), or rotate all168 when inactive.
The same-step probes can affect current selection. Extra observations affect the
posterior but never the original acquisition score. Missing attempts count;
209 attempted current meter/time records per update, with no duplicate rereads.
All16 derived group totals/native masks are separately supplied by a provider
that scans4194 records. No local probe is chosen using an evaluator label.

Reconstruct all112 original episodes including controls, paired within background.
Observe first differences in measurements, innovations, scores, ranking, triggers,
actions, actual IDs, and assimilated values. Track current (nonzero) affected
records, not merely membership in an event-wide set. Exposure does not prove
statistical distinguishability. Use tolerances 1e-9 for diagnostic differences
and1e-8 for reproduction of original score maxima. Keep actual raw values.

Tests use original thresholds and finite controlled observations, including an
available alternate action, hidden changes, opposing signs, budget rejection,
causal ordering and stale/window-restored evidence. They are behavioral checks,
not shock benchmark successes. No shock amplitude will be increased.

Existing alarm calibration is per update, any group or acquired household; the
98th percentile is fitted on Jan3-4 after eight startup steps. The later scored
backgrounds have48 updates each including startup. Report both conventions;
changing the denominator after seeing results cannot fix calibration.

Execution: min(30 minutes, verified36.5600395 remaining), with at least5 minutes
reserved for validation/copy/handoff; original180-minute total and six CPU hours.
Existing closure and inter-stage billing-idle convention persist. Prepare local
code and inspect sources before the first executed diagnostic job starts the
allocation clock. No sealed outcomes, full data scan, new model fit, CPU benchmark,
new hardware, or pod lifecycle action. A correction, if supported by the
reconstructed evidence, needs a dated diagnosis/predicted consequence before edits.
